<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    android:orientation="vertical"
    tools:context=".BulkMessagingActivity">


    <LinearLayout
        android:id="@+id/leaner_search"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <EditText
            android:id="@+id/search_list"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_weight="1"
            android:layout_marginRight="3dp"
            android:background="@drawable/search_edittext"
            android:hint="Search..."
            android:padding="5dp" />

        <Button
            android:id="@+id/filter_l1"
            android:layout_weight="2"
            android:layout_width="match_parent"
            android:text="class"
            android:layout_marginRight="3dp"
            android:background="@drawable/search_edittext_btn"
            android:textColor="@color/white"
            android:layout_height="50dp"/>

        <Button
            android:id="@+id/filter_l2"
            android:layout_weight="2"
            android:text="name"
            android:background="@drawable/search_edittext_btn"
            android:textColor="@color/white"
            android:layout_width="match_parent"
            android:layout_height="50dp"/>

    </LinearLayout>

   <RelativeLayout
       android:layout_width="match_parent"
       android:layout_height="match_parent"
       android:orientation="vertical">

       <ListView
           android:id="@+id/ListView"
           android:layout_width="match_parent"
           android:layout_height="match_parent"
           android:layout_below="@id/multi_send_btn"
           android:layout_marginTop="10dp"
           android:choiceMode="multipleChoice" />

       <Button
           android:id="@+id/multi_send_btn"
           android:layout_width="match_parent"
           android:layout_height="50dp"
           android:layout_marginTop="10dp"
           android:layout_marginBottom="10dp"
           android:textColor="@color/white"
           android:gravity="center"
           android:layout_alignParentEnd="true"
           android:background="@drawable/search_edittext_btn"
           android:text="Send" />

   </RelativeLayout>



</LinearLayout>